#! /bin/bash
# Run the command q as the user q, with ~q/bin in the path (It's where q, *and `qchat`* live. `q` errors if `qchat` isn't on the path)

echo "Hello '${SUDO_USER}', welcome to Q"
sudo su q -c 'PATH=~q/bin:"$PATH"; q'

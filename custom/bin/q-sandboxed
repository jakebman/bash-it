#! /bin/bash
# Run the command q as the user q, in a login shell (to clear out some env variables)
# Alas, this resets the CWD to ~q.

echo "Hello '${SUDO_USER}', welcome to Q"
sudo --login --user q ~q/bin/q
